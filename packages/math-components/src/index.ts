/**
 * @cameronbrady/math-components — main entry.
 *
 * The interactive React components live here. In slice #30 the `Grapher`
 * transformations visualizer and its spec DSL (`slider`, `choose`,
 * `GrapherSpec`) move in. Until then this is a resolvable placeholder so the
 * build, the `exports` map, and downstream consumers have a stable target.
 */
export const PACKAGE_NAME = "@cameronbrady/math-components";
