/**
 * @cameronbrady/math-components/logic — framework-agnostic logic entry.
 *
 * Pure geometry (reflect/translate/rotate/applyTransform) and practice logic
 * (answer grading + progress) move here in slice #29. This entry must never
 * pull React or mafs into its import graph. Placeholder until then.
 */
export const LOGIC_ENTRY = "@cameronbrady/math-components/logic";
