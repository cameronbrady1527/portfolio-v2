---
title: Ratios & Proportions
order: 8
strand: Fractions, Ratios & Proportions
description: A proportion is two equal ratios. Cross-multiply to solve for the missing value — the engine behind unit rates, scale drawings, and similar figures in geometry.
---

A **ratio** compares two quantities; a **proportion** says two ratios are equal.
That single idea — *set two ratios equal and solve* — runs underneath unit
rates, scale drawings, map distances, and the similar figures you'll meet all
through geometry. The tool that cracks every one of them is
**cross-multiplication**.

<SkillCard skill="ratios-proportions" title="Ratios & Proportions">

<SkillStep title="What & why">

A ratio like $3 : 4$ (also written $\frac{3}{4}$) compares two amounts. A
**proportion** sets two ratios equal:

$$\frac{3}{4} = \frac{x}{12}$$

This is true exactly when the **cross-products** match — the two diagonals
multiply to the same thing: $3 \times 12 = 4 \times x$. That fact turns any
proportion with one unknown into a one-step equation. It works because both
sides name the *same* ratio, just scaled differently — the heart of every "if
this, then how much of that" question.

</SkillStep>

<SkillStep title="Watch how it works">

**Cross-multiply, then solve.** For $\frac{3}{4} = \frac{x}{12}$:

$$3 \times 12 = 4 \times x \;\Rightarrow\; 36 = 4x \;\Rightarrow\; x = 9$$

So $\frac{3}{4} = \frac{9}{12}$.

**Real situations are proportions in disguise.** If $3$ apples cost \$1.50, what
do $8$ cost? Set up *price over apples*, equal on both sides:

$$\frac{1.50}{3} = \frac{x}{8}$$

The unit rate is \$0.50 per apple, so $x = 0.50 \times 8 = \$4.00$. Same engine:
equal ratios, solve for the missing piece.

</SkillStep>

<SkillStep title="Reason through one together">

Set up the proportion so matching quantities line up, then cross-multiply.
Predict before you check.

<PredictThenCheck
  prompt="A recipe for 4 people uses 6 eggs. How many eggs are needed for 10 people?"
  choices={["15", "12", "16", "18"]}
>

It's **15**. Keep *eggs over people* on both sides:
$\frac{6}{4} = \frac{x}{10}$. Cross-multiply: $6 \times 10 = 4 \times x$, so
$60 = 4x$ and $x = 15$. (Sanity check: $\frac{6}{4} = 1.5$ eggs per person, and
$1.5 \times 10 = 15$.) Just scaling up by "+6 people → +6 eggs" would miss that
the ratio, not the difference, is what stays constant.

</PredictThenCheck>

</SkillStep>

</SkillCard>
